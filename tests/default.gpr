with "marlowe";

project Default is

   for Source_Dirs use (".", "./generated", "../src");
   for Main use ("test_diamond_1-driver.adb",
                 "test_diamond_1-driver_2.adb",
                 "test_reference_1-driver.adb");

   package Compiler is
      for Default_Switches ("ada") use ("-gnatwa", "-g", "-gnatyaAbCefhicBklnM78oOprsStuxd", "-gnato", "-gnata");
   end Compiler;

   package Builder is
      for Default_Switches ("ada") use ("-s", "-g", "-k", "-j3", "-gnata", "-gnatf");
   end Builder;

end Default;

