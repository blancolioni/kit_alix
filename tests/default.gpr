with "marlowe";

project Default is

   for Source_Dirs use (
     ".",
     "../src"
     );

   package Compiler is
      for Default_Switches ("ada") use ("-gnatwa", "-g", "-O2", "-gnatyaAbCefhicBklnM78oOprsStux");
   end Compiler;

   for Main use ("test_driver.adb");

end Default;

